# CLAUDE.md

## 언어
- 기본적으로 항상 한국어로 응답할 것.
- 사용자가 명시적으로 요청한 경우에만 영어 사용.

## 프로젝트 의미와 현재 전제
- 이 프로젝트는 단순히 결과물을 배포하는 것이 목적이 아니라, 학습자가 전체적인 배포 과정을 처음부터 끝까지 학습하고 이해하기 위한 학습형 프로젝트로 이해할 것.
- 따라서 답변할 때는 "무엇을 하면 되는지"만 말하지 말고, 지금 단계가 전체 배포 흐름에서 어디에 해당하는지까지 학습자가 이해할 수 있게 설명할 것.
- 배포 성공 자체보다 `왜 이 순서로 진행하는지`, `각 기술이 어떤 역할을 하는지`, `이전 단계와 다음 단계가 어떻게 이어지는지`를 이해시키는 것을 중요하게 볼 것.
- 이 프로젝트는 이전에 다른 팀원이 한 차례 Blue/Green 자동배포까지 완료했던 이력이 있으며, 그때 사용했던 기존 배포 관련 파일은 현재 `infra_isolated/` 폴더에 격리되어 있다고 이해할 것.
- `infra_isolated/` 안의 파일은 과거 배포 구성의 참고 자료로 활용할 수 있지만, 현재 작업의 기본 기준이나 최신 운영 정답으로 바로 가정하지 말고 실제 코드 및 현재 문서와 함께 교차 확인할 것.

## 참조 우선순위
- 새 대화를 포함해 프로젝트 기본 컨텍스트는 `AGENTS.md -> CLAUDE.md -> 학습문서/README.md -> 관련 학습문서 -> 실제 코드/설정 파일` 순서로 참조할 것.
- 배포, 인프라, 학습 순서, 운영 원칙 관련 답변 전에는 먼저 `학습문서/배포와인프라/ubot_cicd_study_roadmap.md`를 확인할 것.
- HTTP, HTTPS, 쿠키, CORS, 프록시, WebSocket 관련 답변 전에는 먼저 `학습문서/웹네트워크/http_https_learning_guide.md`를 확인할 것.
- 현재 실제 진행 상태, 어디까지 적용했는지, 다음 단계 판단이 중요한 경우에는 먼저 `학습문서/진행과정/` 아래 문서가 있는지 확인하고 함께 참고할 것.
- 과거 배포 구성이나 이전 배포 방식의 흔적이 필요할 때는 `infra_isolated/`를 참고하되, 참고 자료라는 점을 명확히 인식하고 사용할 것.
- 사용자가 따로 정리하도록 시킨 학습용 문서는 앞으로 `학습문서/` 아래 주제별 폴더에 정리할 것.
- 사용자가 실제 진행한 방향, 적용 상태, 중간 결과를 정리하도록 시킨 문서는 앞으로 `학습문서/진행과정/` 아래에 정리할 것.
- 코드 동작, 포트, 환경변수, 의존성, 배포 방식 등 사실 관계는 반드시 실제 코드와 설정 파일을 최종 기준으로 확인할 것.
- 문서 내용과 실제 코드/설정이 다르면 실제 코드/설정을 우선하고, 차이점은 사용자에게 명확히 설명할 것.

## 응답 가이드라인
- 사용자는 인프라와 배포에 대해 입문자 수준이라고 가정할 것.
- 추상적인 이론만 길게 설명하지 말고, 실용적인 단계별 안내와 함께 배경 이론을 연결해서 설명할 것.
- 특정 기술, 도구, 설정, 아키텍처를 적용하자고 제안할 때는 가능하면 `이 기술이 어떤 이론/개념에서 나온 것인지 -> 그 이론이 현재 기술에 어떻게 이어지는지 -> 왜 지금 프로젝트 상황에 맞는지` 순서로 연결해서 설명할 것.
- 가장 작은 다음 배포 성공 단계를 제시할 것.
- 배포와 직접 관련 없는 한 일반적인 애플리케이션 구조를 다시 설명하지 말 것.
- 민감한 값은 코드에 넣지 말고, 환경변수 또는 외부 설정으로 안내할 것.
- 단일 서버 배포에 먼저 집중할 것.

## 학습 순서 (이 순서를 지킬 것)
1. Docker 기초
2. 환경변수와 런타임 설정
3. Docker Compose 기반 수동 배포
4. 로그, 헬스체크, 재시작, 롤백
5. Nginx / 도메인 / SSL
6. Jenkins CI/CD
7. Blue/Green 배포

`수동 배포 성공`을 첫 번째 마일스톤으로 취급할 것.
수동 배포를 이해하기 전에 CI/CD나 Blue/Green으로 넘어가지 말 것.
